# Feature module placeholder

Feature folders will live here. Example shape:

```
features/
  i-want-to-meet/
    manifest.json
    page.tsx        # or components/
  payments/
  sadaqa/
```

Each module should include a `manifest.json` for attribution and notice-board discovery.
